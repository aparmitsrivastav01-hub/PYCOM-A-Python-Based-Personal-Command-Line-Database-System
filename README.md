# PYCOM
PYCOM is a simple Python + MySQL command-line project that performs CRUD operations using a menu-based interface. It includes input validation, error handling, and an ASCII startup banner. This project demonstrates basic database connectivity and backend logic.
